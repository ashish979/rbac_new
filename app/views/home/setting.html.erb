<div class="margin-bottom">
  <span class="h3">Setting</span>
</div>
<div class="col-md-4">
  <div class="box">
    <div class="box-body">
      <table class='table table-bordered table-hover'>
        <thead>
          <tr><td><%= link_to 'Users', users_path if can? :read, User %></td></tr>
          <tr><td><%= link_to 'Roles', roles_path if can? :read, Role %></td></tr>
        </thead>
      </table>
    </div>
  </div>
</div>